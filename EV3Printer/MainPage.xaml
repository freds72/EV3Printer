<Page
    x:Class="EV3Printer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EV3Printer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:win2d="using:Microsoft.Graphics.Canvas.UI.Xaml"
    DataContext="{Binding Main, Source={StaticResource Locator}}"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="96"/>
        </Grid.ColumnDefinitions>
        <TextBlock Style="{ThemeResource HeaderTextBlockStyle}" Grid.ColumnSpan="2">
			<Run Text="PLOT PRINT3R"/>
        </TextBlock>
        <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding ServerAddress, Mode=TwoWay}"/>
        <Button Grid.Row="1" Grid.Column="1"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Command="{Binding ConnectCommand}" CommandParameter="{Binding ServerAddress, Mode=OneWay}">CONNECT</Button>
        <TextBox Grid.Row="2" Grid.Column="0" Text="{Binding RawCommand, Mode=TwoWay}"/>
        <Button Grid.Row="2" Grid.Column="1"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Command="{Binding SendCommand}" CommandParameter="{Binding RawCommand, Mode=OneWay}">SEND</Button>
        <TextBox Grid.Row="3" Grid.Column="0" Text="{Binding Text2Draw, Mode=TwoWay}"/>
        <Button Grid.Row="3" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">TEXT</Button>
        <win2d:CanvasAnimatedControl Grid.Row="4" Grid.ColumnSpan="2" x:Name="canvas" Draw="canvas_Draw"  Update="canvas_Update" CreateResources="canvas_CreateResources" ClearColor="Black" PointerMoved="canvas_PointerMoved" PointerPressed="canvas_PointerPressed" PointerReleased="canvas_PointerReleased" />
        <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Text="{Binding LastLog}"/>
    </Grid>
</Page>
